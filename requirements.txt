## Minimal core requirements for ML_Project
# These are the packages required to run data preparation, viewer, and baseline training scripts.
# Install PyTorch separately to match your CUDA/drivers (see commented instructions below).

cityscapesscripts==2.2.4
numpy>=1.24
opencv-python>=4.7.0
pillow>=9.0.0
matplotlib>=3.5
scikit-image>=0.19
PyQt5>=5.15
tqdm
requests

# NOTE: Install PyTorch separately matching your CUDA version. Example (CUDA 13.2):
# python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu132
# If you want CPU-only PyTorch, run:
# python -m pip install torch torchvision torchaudio

# Optional (for later phases):
# ultralytics  # required for YOLOv8-seg training (Phase 3)